# 오프라인·무토큰 로컬 임베딩용 (EMBEDDING_BACKEND=local, 예: nlpai-lab/KURE-v1)
#   설치:  pip install -r requirements-local.txt
# GPU가 있으면 자동으로 사용합니다. 없으면 CPU로 동작(대량 문서는 느림).

sentence-transformers>=3.0   # 로컬 임베딩 (KURE-v1/bge-m3 등)
numpy>=1.24                  # 벡터 연산 / 유사도 검색
python-dotenv>=1.0           # .env 로드
pymupdf>=1.24                # PDF 텍스트 추출
olefile>=0.47                # HWP(바이너리) 추출

# 선택: 스캔 PDF OCR (ENABLE_OCR=1) — 별도로 Tesseract 및 한국어 데이터(kor) 설치 필요
# pytesseract>=0.3.10
# pillow>=10.0
